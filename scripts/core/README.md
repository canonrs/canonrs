# Core Scripts

## generate-families.sh
Generates CSS from Rust family tokens via tokens-engine.

**Run**: `./scripts/core/generate-families.sh`

## validate-token-usage.sh
Validates token governance (CI gate).

**Run**: `cargo build && ./scripts/core/validate-token-usage.sh`
