# Rules Module

This module contains Rust type definitions and utilities referenced by the Canon Rules.

## 📖 Documentation

**Full Canon Rules documentation is in:** `/docs/canon/`

This `src/rules/` directory only contains code definitions. 
For the complete rules with examples and explanations, see the markdown files in `/docs/canon/`.
