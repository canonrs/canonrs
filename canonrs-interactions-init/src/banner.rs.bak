//! Banner Init — dismiss via [data-rs-banner-close]

use web_sys::Element;
use canonrs_interactions_core::dom::;
use crate::runtime::{dismiss};

pub fn init(root: Element) {
    dismiss::init(&root, "[data-rs-banner-close]");
}
