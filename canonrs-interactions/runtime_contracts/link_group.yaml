component: link_group
route: /showcase/link-group
interaction: nav
min_listeners: 1

guarantees:
  - no_replay
  - listeners_owned
  - no_orphans

behavior:
  has_items:
    selector: "[data-rs-nav-item]"
    assert: count_gt
    value: 0
