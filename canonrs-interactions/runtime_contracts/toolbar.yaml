component: toolbar
route: /showcase/toolbar
interaction: nav
min_listeners: 1

guarantees:
  - no_replay
  - listeners_owned
  - no_orphans

behavior:
  has_items:
    selector: "[data-rs-toolbar-item]"
    assert: count_gt
    value: 0
